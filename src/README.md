# src 디렉터리

- Maven 표준 레이아웃(`src/main`, `src/test`)을 따르는 소스 코드 루트입니다.
- `src/main/java`에 게임 로직과 JavaFX UI가 위치하며, `src/main/resources`에는 JSON 단어 목록 등 리소스가 들어 있습니다.
- 테스트를 추가할 경우 `src/test/java` 아래에 JUnit 클래스를 생성하고 `mvn test`로 실행하세요.
